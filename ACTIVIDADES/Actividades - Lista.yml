info:
  name: Actividades - Lista
  type: http
  seq: 3

http:
  method: GET
  url: "{{baseUrl}}api/Activities/Activities_Subject_CallCenter?imei={{imei}}"
  params:
    - name: imei
      value: "{{imei}}"
      type: query
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
