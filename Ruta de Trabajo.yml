info:
  name: Ruta de Trabajo
  type: http
  seq: 17

http:
  method: GET
  url: "{{baseUrl}}api/WorkPath?imei={{imei}}"
  params:
    - name: imei
      value: "{{imei}}"
      type: query
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
