info:
  name: Transferencia Gratuita
  type: http
  seq: 13

http:
  method: GET
  url: "{{baseUrl}}api/Ocurrencies/FreeTransfer?imei={{imei}}"
  params:
    - name: imei
      value: "{{imei}}"
      type: query
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
