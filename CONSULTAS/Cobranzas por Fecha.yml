info:
  name: Cobranzas por Fecha
  type: http
  seq: 2

http:
  method: GET
  url: "{{baseUrl}}api/Collections/Date?imei={{imei}}&fecha={{fecha}}"
  params:
    - name: imei
      value: "{{imei}}"
      type: query
    - name: fecha
      value: "{{fecha}}"
      type: query
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
