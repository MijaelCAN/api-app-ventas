info:
  name: Trazabilidad - Ruta
  type: http
  seq: 15

http:
  method: GET
  url: "{{baseUrl}}api/Traceabilities/route?imei={{imei}}&fecha={{fecha}}"
  params:
    - name: imei
      value: "{{imei}}"
      type: query
    - name: fecha
      value: "{{fecha}}"
      type: query
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
