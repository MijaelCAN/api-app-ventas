info:
  name: Efectividad - Quota
  type: http
  seq: 16

http:
  method: GET
  url: "{{baseUrl}}api/Effectiveness/Quote?imei={{imei}}"
  params:
    - name: imei
      value: "{{imei}}"
      type: query
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
