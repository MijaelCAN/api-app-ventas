info:
  name: Ocurriencies - Dispatch
  type: http
  seq: 5

http:
  method: GET
  url: "{{baseUrl}}api/Ocurrencies/Dispatch?imei={{imei}}"
  params:
    - name: imei
      value: "{{imei}}"
      type: query
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
