info:
  name: Precios - Header
  type: http
  seq: 3

http:
  method: GET
  url: "{{baseUrl}}api/PriceList/Header?imei={{imei}}"
  params:
    - name: imei
      value: "{{imei}}"
      type: query
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
