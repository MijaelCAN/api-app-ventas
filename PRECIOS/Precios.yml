info:
  name: Precios
  type: http
  seq: 2

http:
  method: GET
  url: "{{baseUrl}}api/PriceList?imei={{imei}}"
  params:
    - name: imei
      value: "{{imei}}"
      type: query
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
