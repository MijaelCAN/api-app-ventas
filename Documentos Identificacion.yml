info:
  name: Documentos Identificacion
  type: http
  seq: 21

http:
  method: GET
  url: "{{baseUrl}}api/Customers/Type_Identification?imei={{imei}}"
  params:
    - name: imei
      value: "{{imei}}"
      type: query
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
