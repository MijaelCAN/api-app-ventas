info:
  name: Actividad Economica
  type: http
  seq: 9

http:
  method: GET
  url: "{{baseUrl}}api/Customers/Activity_Economic?imei={{imei}}"
  params:
    - name: imei
      value: "{{imei}}"
      type: query
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
