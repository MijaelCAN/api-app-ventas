info:
  name: Prefijos de cliente
  type: http
  seq: 8

http:
  method: GET
  url: "{{baseUrl}}api/Customers/Client_Prefix?imei={{imei}}"
  params:
    - name: imei
      value: "{{imei}}"
      type: query
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
