info:
  name: Supervisor
  type: http
  seq: 1

http:
  method: GET
  url: "{{baseUrl}}api/Superviser?imei={{imei}}&date=2026-06-08"
  params:
    - name: imei
      value: "{{imei}}"
      type: query
    - name: date
      value: 2026-06-08
      type: query
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
