info:
  name: Validacion de Documentos
  type: http
  seq: 18

http:
  method: GET
  url: "{{baseUrl}}api/ValidationDocument?imei={{imei}}"
  params:
    - name: imei
      value: "{{imei}}"
      type: query
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
