info:
  name: Mantenimiento - Encuesta
  type: http
  seq: 23

http:
  method: GET
  url: "{{baseUrl}}api/survey?IdEncuesta=MantenimientoCliente"
  params:
    - name: IdEncuesta
      value: MantenimientoCliente
      type: query
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
