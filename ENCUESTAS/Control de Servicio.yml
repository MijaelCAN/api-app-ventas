info:
  name: Control de Servicio
  type: http
  seq: 1

http:
  method: GET
  url: "{{baseUrl}}api/survey?IdEncuesta=Control de Servicio"
  params:
    - name: IdEncuesta
      value: Control de Servicio
      type: query
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
