info:
  name: TradeMarketing
  type: http
  seq: 22

http:
  method: GET
  url: "{{baseUrl}}api/survey?IdEncuesta=Trade_Marketing"
  params:
    - name: IdEncuesta
      value: Trade_Marketing
      type: query
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
