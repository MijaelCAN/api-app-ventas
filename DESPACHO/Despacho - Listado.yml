info:
  name: Despacho - Listado
  type: http
  seq: 3

http:
  method: GET
  url: "{{baseUrl}}api/Dispatch/List?imei={{imei}}&fecha={{fecha}}"
  params:
    - name: imei
      value: "{{imei}}"
      type: query
    - name: fecha
      value: "{{fecha}}"
      type: query
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
