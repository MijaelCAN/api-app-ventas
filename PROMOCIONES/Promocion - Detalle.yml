info:
  name: Promocion - Detalle
  type: http
  seq: 2

http:
  method: GET
  url: "{{baseUrl}}api/PromotionDetail?imei={{imei}}"
  params:
    - name: imei
      value: "{{imei}}"
      type: query
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
