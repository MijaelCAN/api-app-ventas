info:
  name: Promocion - Header
  type: http
  seq: 1

http:
  method: GET
  url: "{{baseUrl}}api/PromotionHeader?imei={{imei}}"
  params:
    - name: imei
      value: "{{imei}}"
      type: query
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
